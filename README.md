# Para a documentação, consulte o swagger.
# API com TypeORM

Steps to run this project:

1. Run `npm install` command
2. Npm run dev

# API de movies

- Endpoints disponivel no swagger;
- Métodos HTTP: GET, POST, PUT, DELETE
